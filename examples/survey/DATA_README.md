# survey.csv

> Tabular dataset with 5 rows and 6 columns. Covers values from 2025-01-03 to 2025-01-10 in `submitted_at`. Overall null rate is 10.0%. Includes 1 identifier-like column.

## At a glance

| | |
|---|---|
| Rows | 5 |
| Columns | 6 |
| File size | 363 B |
| Duplicate rows | 0 |
| Overall null rate | 10.0% |
| Generated | 2026-04-03 |

## Columns

| Column | Type | Nulls | Description |
|---|---|---|---|
| `respondent_id` | integer | 0.0% | Identifier that likely links to a related `respondent` record. |
| `submitted_at` | datetime | 0.0% | Date field; spans 2025-01-03 to 2025-01-10. |
| `satisfaction_score` | integer | 0.0% | Numeric field; ranges from 6 to 10. |
| `plan_type` | string | 0.0% | Category-like field; common values include `pro` (40%), `basic` (40%), `enterprise` (20%). |
| `follow_up_email` | string | 40.0% | Text field; common values include `alex@example.com` (20%), `sam@example.com` (20%), `ops@example.com` (20%). 40.0% nulls. |
| `comment` | string | 20.0% | Text field; common values include `Fast setup and clear docs` (20%), `Would like better export options` (20%), `Team adopted it quickly` (20%). 20.0% nulls. |

## Data quality notes

- `follow_up_email`: 40.0% null; review before analysis.
- `follow_up_email`: may contain personal data; review privacy requirements before sharing.
- `comment`: 20.0% null; review before analysis.

## How to load

```python
import pandas as pd
df = pd.read_csv("survey.csv")
df["submitted_at"] = pd.to_datetime(df["submitted_at"])
```

---
*Generated by datareadme*