# transactions.csv

> Tabular dataset with 4 rows and 5 columns. Covers values from 2024-01-01 to 2024-02-10 in `created_at`. Includes 1 identifier-like column.

## At a glance

| | |
|---|---|
| Rows | 4 |
| Columns | 5 |
| File size | 240 B |
| Duplicate rows | 1 |
| Overall null rate | 0.0% |
| Generated | 2026-04-03 |

## Columns

| Column | Type | Nulls | Description |
|---|---|---|---|
| `customer_id` | integer | 0.0% | Identifier that likely links to a related `customer` record. |
| `created_at` | datetime | 0.0% | Creation timestamp; spans 2024-01-01 to 2024-02-10. |
| `status` | string | 0.0% | Status field; common values include `complete` (75%), `pending` (25%). |
| `total_amount` | float | 0.0% | Monetary value; ranges from $19.99 to $49.50. May be derived from other fields. |
| `email` | string | 0.0% | Text field; common values include `charlie@example.com` (50%), `amy@example.com` (25%), `bob@example.com` (25%). |

## Data quality notes

- `total_amount`: may be a derived field; verify before treating it as source-of-truth data.
- `email`: may contain personal data; review privacy requirements before sharing.
- 1 duplicate rows detected.

## How to load

```python
import pandas as pd
df = pd.read_csv("transactions.csv")
df["created_at"] = pd.to_datetime(df["created_at"])
```

---
*Generated by datareadme*